## Prerequisites

If you haven't prepared the examples repository yet, you can do so from the project root:

```bash
npm install
```

## Init modules only

Start from terminal:

```bash
cd examples/00*
npm run start:dev
```
