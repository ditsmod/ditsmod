import { AppModule } from '#app/app.module.js';

describe('00-init-modules-only', () => {
  describe('injector-scoped controller', () => {
    it('works with GET method', async () => {
      
    });
  });
});
