export class AssertConfig {
  maxId: number = 999999999999999;
  /**
   * If the length of the text passed as a parameter exceeds this value,
   * the parameter will be written partially and will have an ellipsis at the end.
   */
  maxLengthLogParam: number = 100;
}
