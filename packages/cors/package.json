{
  "name": "@ditsmod/cors",
  "version": "2.0.0-beta.9",
  "repository": {
    "type": "git",
    "url": "https://github.com/ditsmod/ditsmod.git"
  },
  "description": "Ditsmod module to support CORS and OPTIONS requests",
  "main": "dist/index",
  "typings": "dist/index",
  "directories": {
    "lib": "src"
  },
  "scripts": {
    "test": "jest",
    "build": "rm -rf dist/* && tsc -b -f"
  },
  "keywords": [
    "ditsmod",
    "cors",
    "options"
  ],
  "author": "Костя Третяк",
  "license": "MIT",
  "dependencies": {
    "@ts-stack/cookies": "^1.0.2",
    "@ts-stack/cors": "^1.3.0"
  },
  "peerDependencies": {
    "@ts-stack/di": "^2.0.0-beta.5",
    "@ditsmod/core": "^2.29.0"
  }
}
